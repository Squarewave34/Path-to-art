<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Welcome to completed projects</h1>

  <% projects.forEach((project)=>{ %>
    <% if(project.status==="completed") {%>

      <div class="flex">
        <div class="card flex">
          <a href="/completedProjects/project/<%= project._id %>"><%= project.name %></a>
        </div>
      </div>

    <br>
  <% }}) %>

</body>
</html>